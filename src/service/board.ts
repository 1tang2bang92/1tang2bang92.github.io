/**
 * BOARD
 * UID: int
 * TITLE: string
 * BODY: string | XML
 * UPDATE_AT: datetime
 */

const getPosts = () => {
    return {
        'uid': 1,
        'title': 'hello',
        'body': 'world!',
        'update_at': 'time'
    }
}